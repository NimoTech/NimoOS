# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com
project_name: nimoos
before:
  hooks:
    - go generate
    - go run github.com/google/go-licenses@latest check . --disallowed_types=restricted
    - go mod tidy
    - go test -race -v ./...

builds:
  - id: nimoos-amd64
    binary: build/sysroot/usr/bin/nimoos
    hooks:
      post:
        - upx --best --lzma -v --no-progress "{{ .Path }}"
    env:
      - CC=x86_64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -X github.com/NimoTech/NimoOS/drivers/google_drive.client_id={{.Env.GoogleID}}
      - -X github.com/NimoTech/NimoOS/drivers/google_drive.client_secret={{.Env.GoogleSecret}}
      - -X github.com/NimoTech/NimoOS/drivers/onedrive.client_id={{.Env.OneDriveID}}
      - -X github.com/NimoTech/NimoOS/drivers/onedrive.client_secret={{.Env.OneDriveSecret}}
      - -X github.com/NimoTech/NimoOS/drivers/dropbox.app_key={{.Env.DropboxKey}}
      - -X github.com/NimoTech/NimoOS/drivers/dropbox.app_secret={{.Env.DropboxSecret}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - amd64
  - id: nimoos-arm64
    binary: build/sysroot/usr/bin/nimoos
    # hooks:
    #   post:
    #     - upx --best --lzma -v --no-progress "{{ .Path }}"
    env:
      - CC=aarch64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -X github.com/NimoTech/NimoOS/drivers/google_drive.client_id={{.Env.GoogleID}}
      - -X github.com/NimoTech/NimoOS/drivers/google_drive.client_secret={{.Env.GoogleSecret}}
      - -X github.com/NimoTech/NimoOS/drivers/onedrive.client_id={{.Env.OneDriveID}}
      - -X github.com/NimoTech/NimoOS/drivers/onedrive.client_secret={{.Env.OneDriveSecret}}
      - -X github.com/NimoTech/NimoOS/drivers/dropbox.app_key={{.Env.DropboxKey}}
      - -X github.com/NimoTech/NimoOS/drivers/dropbox.app_secret={{.Env.DropboxSecret}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - arm64
  - id: nimoos-arm-7
    binary: build/sysroot/usr/bin/nimoos
    hooks:
      post:
        - upx --best --lzma -v --no-progress "{{ .Path }}"
    env:
      - CC=arm-linux-gnueabihf-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -X github.com/NimoTech/NimoOS/drivers/google_drive.client_id={{.Env.GoogleID}}
      - -X github.com/NimoTech/NimoOS/drivers/google_drive.client_secret={{.Env.GoogleSecret}}
      - -X github.com/NimoTech/NimoOS/drivers/onedrive.client_id={{.Env.OneDriveID}}
      - -X github.com/NimoTech/NimoOS/drivers/onedrive.client_secret={{.Env.OneDriveSecret}}
      - -X github.com/NimoTech/NimoOS/drivers/dropbox.app_key={{.Env.DropboxKey}}
      - -X github.com/NimoTech/NimoOS/drivers/dropbox.app_secret={{.Env.DropboxSecret}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - arm
    goarm:
      - "7"
  - id: nimoos-riscv64
    binary: build/sysroot/usr/bin/nimoos
    env:
      - CC=riscv64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -X github.com/NimoTech/NimoOS/drivers/google_drive.client_id={{.Env.GoogleID}}
      - -X github.com/NimoTech/NimoOS/drivers/google_drive.client_secret={{.Env.GoogleSecret}}
      - -X github.com/NimoTech/NimoOS/drivers/onedrive.client_id={{.Env.OneDriveID}}
      - -X github.com/NimoTech/NimoOS/drivers/onedrive.client_secret={{.Env.OneDriveSecret}}
      - -X github.com/NimoTech/NimoOS/drivers/dropbox.app_key={{.Env.DropboxKey}}
      - -X github.com/NimoTech/NimoOS/drivers/dropbox.app_secret={{.Env.DropboxSecret}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - riscv64
  - id: nimoos-migration-tool-amd64
    binary: build/sysroot/usr/bin/nimoos-migration-tool
    hooks:
      post:
        - upx --best --lzma -v --no-progress "{{ .Path }}"
    main: ./cmd/migration-tool
    env:
      - CC=x86_64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - amd64
  - id: nimoos-migration-tool-arm64
    binary: build/sysroot/usr/bin/nimoos-migration-tool
    # hooks:
    #   post:
    #     - upx --best --lzma -v --no-progress "{{ .Path }}"
    main: ./cmd/migration-tool
    env:
      - CC=aarch64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - arm64
  - id: nimoos-migration-tool-arm-7
    binary: build/sysroot/usr/bin/nimoos-migration-tool
    hooks:
      post:
        - upx --best --lzma -v --no-progress "{{ .Path }}"
    main: ./cmd/migration-tool
    env:
      - CC=arm-linux-gnueabihf-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - arm
    goarm:
      - "7"
  - id: nimoos-migration-tool-riscv64
    binary: build/sysroot/usr/bin/nimoos-migration-tool
    main: ./cmd/migration-tool
    env:
      - CC=riscv64-linux-gnu-gcc
    ldflags:
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
      - -s
      - -w
      - -extldflags "-static"
    tags:
      - musl
      - netgo
      - osusergo
    goos:
      - linux
    goarch:
      - riscv64
archives:
  - name_template:  >- 
      {{ .Os }}-{{- if eq .Arch "arm" }}arm-7{{- else }}{{ .Arch }}{{- end }}-{{ .ProjectName }}-v{{ .Version }}
    id: nimoos
    builds:
      - nimoos-amd64
      - nimoos-arm64
      - nimoos-arm-7
      - nimoos-riscv64
    files:
      - build/**/*
  - name_template: >-
      {{ .Os }}-{{- if eq .Arch "arm" }}arm-7{{- else }}{{ .Arch }}{{- end }}-{{ .ProjectName }}-migration-tool-v{{ .Version }}
    id: nimoos-migration-tool
    builds:
      - nimoos-migration-tool-amd64
      - nimoos-migration-tool-arm64
      - nimoos-migration-tool-arm-7
      - nimoos-migration-tool-riscv64
    files:
      - build/sysroot/etc/**/*
checksum:
  name_template: "checksums.txt"
snapshot:
  name_template: "{{ incpatch .Version }}"
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
# release:
#   github:
#     owner: NimoTech
#     name: NimoOS
#   draft: true
#   prerelease: auto
#   mode: replace
#   name_template: "v{{ .Version }}"
release:
  github:
    owner: NimoTech
    name: NimoOS
  draft: true
  prerelease: auto
  mode: replace
  name_template: "v{{ .Version }}"
