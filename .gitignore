### Go template
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

### Example user template template
### Example user template

# IntelliJ project files
.idea
.vscode
*.iml
out
gen
/logs/
/sql/
/out/
/db/
/conf/conf.ini
/conf/conf.conf
/conf/conf.json
__debug_bin
main
github.com
.all-contributorsrc
dist
NimoOS
nimoos
/codegen

# System Files
.DS_Store
node_modules

# Claude Code local guidance — not for upload
CLAUDE.md

# Build artifacts. Three compiled binaries (casa, casa_test, casaos, ~30MB each)
# were tracked here. Go binaries embed the build machine's module paths, so they
# also carried ~1000 filesystem path strings and a developer username.
casa
casa_test
casaos
nimoos
