name: Collect Code Coverage

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Checkout NimoOS-Common
        uses: actions/checkout@v3
        with:
          repository: NimoTech/NimoOS-Common
          path: ../NimoOS-Common
          token: ${{ secrets.API_TOKEN_GITHUB }}
      - name: Checkout NimoOS-MessageBus
        uses: actions/checkout@v3
        with:
          repository: NimoTech/NimoOS-MessageBus
          path: ../NimoOS-MessageBus
          token: ${{ secrets.API_TOKEN_GITHUB }}
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.22"
      - name: generate OPENAPI
        run: go generate
      - name: Run coverage
        run: go test -race -failfast -coverprofile=coverage.txt -covermode=atomic -v ./...
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
